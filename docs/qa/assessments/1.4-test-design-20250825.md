# Test Design: Story 1.4 - CSS Architecture and Base Styles

Date: 2025-08-25
Designer: Quinn (Test Architect)

## Test Strategy Overview

- Total test scenarios: 16
- Unit tests: 10 (63%)
- Integration tests: 4 (25%)
- E2E tests: 2 (12%)
- Priority distribution: P0: 6, P1: 7, P2: 3

**Testing Focus**: CSS architecture validation, build process integration, BEM methodology compliance, and design system consistency. Risk-based approach targeting CSS architecture violations and build failures.

## Test Scenarios by Acceptance Criteria

### AC1: CSS Organization - Logical Structure (base, components, layout, utilities)

#### Test Risk Analysis
- **RISK-002**: Build Process Failures - CSS not compiling or loading
- **RISK-001**: CSS Architecture Violations - Inconsistent file organization

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-001 | Unit        | P0       | CSS directory structure validation      | Critical architecture foundation        |
| 1.4-UNIT-002 | Unit        | P0       | All required CSS files exist           | Build dependency validation             |
| 1.4-INT-001  | Integration | P0       | CSS import resolution in main.css      | Build process integration               |
| 1.4-INT-002  | Integration | P1       | CSS loading order is correct           | Cascade and specificity management     |

### AC2: Zero Inline Styles - No style attributes in HTML/Elm

#### Test Risk Analysis
- **RISK-001**: CSS Architecture Violations - Inline styles breaking methodology
- **RISK-004**: BEM Methodology Violations - Development shortcuts bypassing standards

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-003 | Unit        | P0       | Static analysis: No style attributes    | Critical architecture compliance        |
| 1.4-INT-003  | Integration | P1       | Build-time inline style validation     | Automated enforcement                   |
| 1.4-E2E-001  | E2E         | P1       | Runtime DOM style attribute check       | Live validation                         |

### AC3: CSS Naming Convention - BEM Methodology

#### Test Risk Analysis
- **RISK-004**: BEM Methodology Violations - Naming convention breakdown
- **RISK-003**: Design System Inconsistency - Non-standard class naming

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-004 | Unit        | P0       | BEM class name validation              | Core methodology compliance             |
| 1.4-UNIT-005 | Unit        | P1       | CSS selector specificity check         | Prevent cascade conflicts               |
| 1.4-UNIT-006 | Unit        | P2       | Utility class naming consistency       | Design system adherence                 |

### AC4: Base Design System - Typography, colors, spacing

#### Test Risk Analysis
- **RISK-003**: Design System Inconsistency - Components not using design tokens
- **RISK-005**: Performance Degradation - Hardcoded values instead of variables

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-007 | Unit        | P0       | CSS custom properties defined          | Design system foundation                |
| 1.4-UNIT-008 | Unit        | P1       | Design token usage validation          | Consistency enforcement                 |
| 1.4-INT-004  | Integration | P1       | Cross-component variable inheritance   | System-wide design coherence            |

### AC5: Modern Card Components - Following design patterns

#### Test Risk Analysis
- **RISK-003**: Design System Inconsistency - Card styles not using tokens
- **RISK-005**: Performance Degradation - Inefficient CSS selectors

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-009 | Unit        | P1       | Card component CSS property validation | Modern design pattern compliance        |
| 1.4-E2E-002  | E2E         | P2       | Visual regression: Card appearance     | Design consistency verification         |

### AC6: CSS Framework Integration - Custom CSS with BEM

#### Test Risk Analysis
- **RISK-002**: Build Process Failures - Framework integration conflicts
- **RISK-004**: BEM Methodology Violations - External framework interference

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-010 | Unit        | P2       | No external CSS framework conflicts    | Pure custom CSS validation             |

### AC7: Build Process Integration - CSS compilation with Elm

#### Test Risk Analysis
- **RISK-002**: Build Process Failures - CSS not compiling or loading
- **RISK-005**: Performance Degradation - CSS bundle size or load times

#### Scenarios

| ID           | Level       | Priority | Test                                    | Justification                           |
| ------------ | ----------- | -------- | --------------------------------------- | --------------------------------------- |
| 1.4-UNIT-011 | Unit        | P0       | Webpack CSS configuration validation   | Build system foundation                 |
| 1.4-INT-005  | Integration | P1       | CSS hot reload functionality           | Development experience                  |

## Risk Coverage Mapping

- **RISK-001: CSS Architecture Violations** → 1.4-UNIT-001, 1.4-UNIT-002, 1.4-UNIT-003, 1.4-INT-001
- **RISK-002: Build Process Failures** → 1.4-INT-001, 1.4-INT-002, 1.4-UNIT-011, 1.4-INT-005
- **RISK-003: Design System Inconsistency** → 1.4-UNIT-007, 1.4-UNIT-008, 1.4-INT-004, 1.4-UNIT-006
- **RISK-004: BEM Methodology Violations** → 1.4-UNIT-004, 1.4-UNIT-005, 1.4-UNIT-003, 1.4-UNIT-010
- **RISK-005: Performance Degradation** → 1.4-UNIT-008, 1.4-UNIT-009, 1.4-INT-005

## Recommended Execution Order

### Phase 1: P0 Critical Tests (Fail Fast)
1. 1.4-UNIT-001 (CSS directory structure validation)
2. 1.4-UNIT-002 (All required CSS files exist)
3. 1.4-INT-001 (CSS import resolution in main.css)
4. 1.4-UNIT-003 (Static analysis: No style attributes)
5. 1.4-UNIT-004 (BEM class name validation)
6. 1.4-UNIT-007 (CSS custom properties defined)
7. 1.4-UNIT-011 (Webpack CSS configuration validation)

### Phase 2: P1 Important Tests
8. 1.4-INT-002 (CSS loading order is correct)
9. 1.4-INT-003 (Build-time inline style validation)
10. 1.4-E2E-001 (Runtime DOM style attribute check)
11. 1.4-UNIT-005 (CSS selector specificity check)
12. 1.4-UNIT-008 (Design token usage validation)
13. 1.4-INT-004 (Cross-component variable inheritance)
14. 1.4-UNIT-009 (Card component CSS property validation)
15. 1.4-INT-005 (CSS hot reload functionality)

### Phase 3: P2 Nice-to-Have Tests
16. 1.4-UNIT-006 (Utility class naming consistency)
17. 1.4-E2E-002 (Visual regression: Card appearance)
18. 1.4-UNIT-010 (No external CSS framework conflicts)

## Detailed Test Scenarios

### Unit Test Specifications

#### 1.4-UNIT-001: CSS Directory Structure Validation
**Priority**: P0  
**Risk**: RISK-001 (CSS Architecture Violations)
```javascript
describe('CSS Directory Structure', () => {
  test('should have all required CSS directories', () => {
    const requiredDirs = ['base', 'components', 'layout', 'utilities', 'pages'];
    requiredDirs.forEach(dir => {
      expect(fs.existsSync(`assets/styles/${dir}`)).toBe(true);
    });
  });
});
```

#### 1.4-UNIT-002: All Required CSS Files Exist
**Priority**: P0  
**Risk**: RISK-002 (Build Process Failures)
```javascript
describe('Required CSS Files', () => {
  test('should have all required CSS files present', () => {
    const requiredFiles = [
      'base/reset.css', 'base/variables.css', 'base/typography.css',
      'components/buttons.css', 'components/cards.css', 'components/forms.css',
      'layout/containers.css', 'layout/grid.css', 'layout/header.css',
      'utilities/spacing.css', 'utilities/colors.css', 'utilities/helpers.css'
    ];
    requiredFiles.forEach(file => {
      expect(fs.existsSync(`assets/styles/${file}`)).toBe(true);
    });
  });
});
```

#### 1.4-UNIT-003: Static Analysis - No Style Attributes
**Priority**: P0  
**Risk**: RISK-001 (CSS Architecture Violations)
```javascript
describe('Inline Style Detection', () => {
  test('should find no style attributes in Elm files', () => {
    const elmFiles = glob.sync('src/**/*.elm');
    elmFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      expect(content).not.toMatch(/Html\.Attributes\.style/);
      expect(content).not.toMatch(/style\s*=/);
    });
  });

  test('should find no style attributes in HTML files', () => {
    const htmlFiles = glob.sync('public/**/*.html');
    htmlFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      expect(content).not.toMatch(/style\s*=/);
    });
  });
});
```

#### 1.4-UNIT-004: BEM Class Name Validation
**Priority**: P0  
**Risk**: RISK-004 (BEM Methodology Violations)
```javascript
describe('BEM Naming Convention', () => {
  test('should follow BEM methodology in CSS files', () => {
    const cssFiles = glob.sync('assets/styles/**/*.css');
    cssFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      const classNames = extractClassNames(content);
      
      classNames.forEach(className => {
        // Skip utility classes (prefixed with u-)
        if (className.startsWith('u-')) return;
        
        // Validate BEM structure: block__element--modifier
        const bemPattern = /^[a-z][a-z0-9]*(-[a-z0-9]+)*(__[a-z][a-z0-9]*(-[a-z0-9]+)*)?(--[a-z][a-z0-9]*(-[a-z0-9]+)*)?$/;
        expect(className).toMatch(bemPattern);
      });
    });
  });
});
```

#### 1.4-UNIT-005: CSS Selector Specificity Check
**Priority**: P1  
**Risk**: RISK-004 (BEM Methodology Violations)
```javascript
describe('CSS Specificity', () => {
  test('should maintain low specificity for maintainability', () => {
    const cssFiles = glob.sync('assets/styles/**/*.css');
    cssFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      const selectors = extractSelectors(content);
      
      selectors.forEach(selector => {
        const specificity = calculateSpecificity(selector);
        // BEM should keep specificity low (max 20 for most cases)
        expect(specificity.total).toBeLessThan(30);
      });
    });
  });
});
```

#### 1.4-UNIT-006: Utility Class Naming Consistency
**Priority**: P2  
**Risk**: RISK-003 (Design System Inconsistency)
```javascript
describe('Utility Class Naming', () => {
  test('should prefix utility classes with u-', () => {
    const utilityFiles = glob.sync('assets/styles/utilities/*.css');
    utilityFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      const classNames = extractClassNames(content);
      
      classNames.forEach(className => {
        expect(className).toMatch(/^u-[a-z]/);
      });
    });
  });
});
```

#### 1.4-UNIT-007: CSS Custom Properties Defined
**Priority**: P0  
**Risk**: RISK-003 (Design System Inconsistency)
```javascript
describe('CSS Custom Properties', () => {
  test('should define all required design system variables', () => {
    const variablesContent = fs.readFileSync('assets/styles/base/variables.css', 'utf8');
    
    const requiredVariables = [
      '--color-primary', '--color-secondary', '--color-background',
      '--spacing-xs', '--spacing-sm', '--spacing-md', '--spacing-lg',
      '--font-size-base', '--border-radius', '--box-shadow'
    ];
    
    requiredVariables.forEach(variable => {
      expect(variablesContent).toMatch(new RegExp(`${variable}:\\s*[^;]+;`));
    });
  });
});
```

#### 1.4-UNIT-008: Design Token Usage Validation
**Priority**: P1  
**Risk**: RISK-003 (Design System Inconsistency)
```javascript
describe('Design Token Usage', () => {
  test('should use CSS variables instead of hardcoded values', () => {
    const cssFiles = glob.sync('assets/styles/{components,layout,pages}/*.css');
    cssFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      
      // Check for hardcoded colors (should use var(--color-*))
      expect(content).not.toMatch(/#[0-9a-fA-F]{3,6}/);
      
      // Check for hardcoded spacing (should use var(--spacing-*))
      const hardcodedSpacing = /(?:margin|padding|gap):\s*(?:\d+(?:\.\d+)?(?:px|rem|em))/g;
      const matches = content.match(hardcodedSpacing) || [];
      
      // Allow a few hardcoded values for special cases, but flag excessive use
      expect(matches.length).toBeLessThan(5);
    });
  });
});
```

#### 1.4-UNIT-009: Card Component CSS Property Validation
**Priority**: P1  
**Risk**: RISK-005 (Performance Degradation)
```javascript
describe('Card Component Styles', () => {
  test('should implement modern design patterns', () => {
    const cardContent = fs.readFileSync('assets/styles/components/cards.css', 'utf8');
    
    // Check for modern design properties
    expect(cardContent).toMatch(/border-radius/);
    expect(cardContent).toMatch(/box-shadow/);
    expect(cardContent).toMatch(/transition/);
    
    // Check for CSS variables usage
    expect(cardContent).toMatch(/var\(--[a-z-]+\)/);
  });
});
```

#### 1.4-UNIT-010: No External CSS Framework Conflicts
**Priority**: P2  
**Risk**: RISK-004 (BEM Methodology Violations)
```javascript
describe('CSS Framework Integration', () => {
  test('should not import external CSS frameworks', () => {
    const mainCssContent = fs.readFileSync('assets/styles/main.css', 'utf8');
    const packageJson = require('../../../package.json');
    
    // Check no external CSS imports
    expect(mainCssContent).not.toMatch(/@import.*https?:/);
    expect(mainCssContent).not.toMatch(/@import.*node_modules/);
    
    // Check no CSS framework dependencies
    const frameworkNames = ['bootstrap', 'tailwind', 'bulma', 'foundation'];
    frameworkNames.forEach(framework => {
      expect(packageJson.dependencies).not.toHaveProperty(framework);
    });
  });
});
```

#### 1.4-UNIT-011: Webpack CSS Configuration Validation
**Priority**: P0  
**Risk**: RISK-002 (Build Process Failures)
```javascript
describe('Webpack CSS Configuration', () => {
  test('should have proper CSS loader configuration', () => {
    const webpackConfig = require('../../../webpack.config.js')({}, { mode: 'development' });
    
    const cssRule = webpackConfig.module.rules.find(rule => rule.test.test('.css'));
    expect(cssRule).toBeDefined();
    expect(cssRule.use).toContain('css-loader');
  });
});
```

### Integration Test Specifications

#### 1.4-INT-001: CSS Import Resolution in main.css
**Priority**: P0  
**Risk**: RISK-002 (Build Process Failures)
```javascript
describe('CSS Import Resolution', () => {
  test('should resolve all CSS imports in main.css', async () => {
    const mainCssContent = fs.readFileSync('assets/styles/main.css', 'utf8');
    const importPaths = extractImportPaths(mainCssContent);
    
    for (const importPath of importPaths) {
      const fullPath = path.resolve('assets/styles', importPath);
      expect(fs.existsSync(fullPath)).toBe(true);
      
      // Test that imported files are valid CSS
      const importedContent = fs.readFileSync(fullPath, 'utf8');
      expect(() => parseCSS(importedContent)).not.toThrow();
    }
  });
});
```

#### 1.4-INT-002: CSS Loading Order is Correct
**Priority**: P1  
**Risk**: RISK-002 (Build Process Failures)
```javascript
describe('CSS Loading Order', () => {
  test('should load CSS in correct cascade order', () => {
    const mainCssContent = fs.readFileSync('assets/styles/main.css', 'utf8');
    const importOrder = extractImportOrder(mainCssContent);
    
    // Verify order: base → layout → components → utilities → pages
    const expectedOrder = ['base/', 'layout/', 'components/', 'utilities/', 'pages/'];
    let currentIndex = 0;
    
    importOrder.forEach(importPath => {
      const category = expectedOrder.find(cat => importPath.startsWith(cat));
      if (category) {
        const categoryIndex = expectedOrder.indexOf(category);
        expect(categoryIndex).toBeGreaterThanOrEqual(currentIndex);
        currentIndex = categoryIndex;
      }
    });
  });
});
```

#### 1.4-INT-003: Build-time Inline Style Validation
**Priority**: P1  
**Risk**: RISK-001 (CSS Architecture Violations)
```javascript
describe('Build-time Style Validation', () => {
  test('should fail build if inline styles are detected', async () => {
    // Create temp file with inline style
    const tempFile = 'src/temp-test.elm';
    fs.writeFileSync(tempFile, `
      module TempTest exposing (..)
      import Html exposing (div)
      import Html.Attributes exposing (style)
      
      view = div [ style "color" "red" ] []
    `);
    
    try {
      // Run build process
      const buildResult = await runCommand('npm run build');
      
      // Should detect inline style and warn/fail
      expect(buildResult.stderr).toMatch(/inline.*style/i);
    } finally {
      // Clean up temp file
      fs.unlinkSync(tempFile);
    }
  });
});
```

#### 1.4-INT-004: Cross-component Variable Inheritance
**Priority**: P1  
**Risk**: RISK-003 (Design System Inconsistency)
```javascript
describe('CSS Variable Inheritance', () => {
  test('should inherit design system variables across components', () => {
    const componentFiles = glob.sync('assets/styles/components/*.css');
    const variablesContent = fs.readFileSync('assets/styles/base/variables.css', 'utf8');
    const definedVariables = extractVariableNames(variablesContent);
    
    componentFiles.forEach(file => {
      const content = fs.readFileSync(file, 'utf8');
      const usedVariables = extractUsedVariables(content);
      
      usedVariables.forEach(variable => {
        expect(definedVariables).toContain(variable);
      });
    });
  });
});
```

#### 1.4-INT-005: CSS Hot Reload Functionality
**Priority**: P1  
**Risk**: RISK-005 (Performance Degradation)
```javascript
describe('CSS Hot Reload', () => {
  test('should hot reload CSS changes in development', async () => {
    // Start dev server
    const server = await startDevServer();
    
    try {
      // Make CSS change
      const originalContent = fs.readFileSync('assets/styles/components/cards.css', 'utf8');
      const modifiedContent = originalContent.replace('color: var(--color-text)', 'color: var(--color-primary)');
      fs.writeFileSync('assets/styles/components/cards.css', modifiedContent);
      
      // Wait for hot reload
      await waitForMs(1000);
      
      // Verify change was hot reloaded (check dev server logs or browser)
      const logs = await getDevServerLogs();
      expect(logs).toMatch(/hot.*reload|hmr/i);
      
      // Restore original content
      fs.writeFileSync('assets/styles/components/cards.css', originalContent);
    } finally {
      await stopDevServer(server);
    }
  });
});
```

### E2E Test Specifications

#### 1.4-E2E-001: Runtime DOM Style Attribute Check
**Priority**: P1  
**Risk**: RISK-001 (CSS Architecture Violations)
```javascript
describe('Runtime Style Validation', () => {
  test('should have no inline style attributes in rendered DOM', () => {
    cy.visit('/');
    
    // Check that no elements have inline styles
    cy.get('*').should('not.have.attr', 'style');
    
    // Navigate to tool pages and verify
    cy.get('[data-cy="data-extractor-card"]').click();
    cy.get('*').should('not.have.attr', 'style');
    
    cy.go('back');
    cy.get('[data-cy="data-merger-card"]').click();
    cy.get('*').should('not.have.attr', 'style');
  });
});
```

#### 1.4-E2E-002: Visual Regression - Card Appearance
**Priority**: P2  
**Risk**: RISK-003 (Design System Inconsistency)
```javascript
describe('Visual Regression Testing', () => {
  test('should maintain consistent card design patterns', () => {
    cy.visit('/');
    
    // Take screenshot of tool cards for visual comparison
    cy.get('.tool-cards-container').matchImageSnapshot('tool-cards-desktop');
    
    // Test mobile responsive design
    cy.viewport(375, 667);
    cy.get('.tool-cards-container').matchImageSnapshot('tool-cards-mobile');
    
    // Test hover states
    cy.viewport(1024, 768);
    cy.get('[data-cy="data-extractor-card"]').trigger('mouseover');
    cy.get('.tool-cards-container').matchImageSnapshot('tool-cards-hover');
  });
});
```

## CSS-Specific Testing Utilities

The following utility functions support the test implementations:

### Static Analysis Utilities
```javascript
function extractClassNames(cssContent) {
  const classRegex = /\.([a-zA-Z][a-zA-Z0-9_-]*)/g;
  const matches = cssContent.match(classRegex) || [];
  return matches.map(match => match.slice(1)); // Remove leading dot
}

function calculateSpecificity(selector) {
  // Implementation for CSS specificity calculation
  // Returns { ids: number, classes: number, tags: number, total: number }
}

function extractVariableNames(cssContent) {
  const variableRegex = /--([\w-]+):/g;
  const matches = [...cssContent.matchAll(variableRegex)];
  return matches.map(match => `--${match[1]}`);
}

function extractUsedVariables(cssContent) {
  const usageRegex = /var\((--[\w-]+)\)/g;
  const matches = [...cssContent.matchAll(usageRegex)];
  return matches.map(match => match[1]);
}
```

### Build Process Testing Utilities
```javascript
function runCommand(command) {
  return new Promise((resolve, reject) => {
    const proc = exec(command, (error, stdout, stderr) => {
      resolve({ error, stdout, stderr });
    });
  });
}

async function startDevServer() {
  // Start webpack dev server for testing
}

async function getDevServerLogs() {
  // Retrieve dev server logs for hot reload verification
}
```

This comprehensive test design ensures CSS architecture quality, build process reliability, and design system consistency while focusing on the specific risks and challenges of CSS-based development.